apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: my-ingress
spec:
  rules:
  - host: fast-api-dida.com
    http:
      paths:
      - pathType: Prefix
        path: /
        backend:
          service:
            name: fast-api-service
            port:
              number: 80
